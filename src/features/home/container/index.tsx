import {motion, AnimatePresence} from 'framer-motion';
import {useSnapshot} from 'valtio'

import {headContentAnimation, headContainerAnimation, headTextAnimation} from '../../../config/motion.ts'

const Home = () => {
  return (
    <div>Home</div>
  )
}

export default Home